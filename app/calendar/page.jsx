import React from 'react'

const page = () => {
  return (
    <div>welcome to calendar page!</div>
    // i can add a calender that allow me to add plan,event and taskes, 
    // then separe with tag, like plan, evnet and taskes
    // then show them 
  )
}

export default page